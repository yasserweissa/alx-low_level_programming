#include "main.h"

/**
 * _strlen - returns length of a string
 * @s: string
 * Return: length of s
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0';
		length++;
	return (length);
}
