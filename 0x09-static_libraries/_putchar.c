#include <unistd.h>
#include "main.h"

/**
 * _putchar - [0] writes the character c to stdout
 * @c: The character to print
 * Return: On success 1, error, -1 is returned, and errno is set appropriately.
 **/
int _putchar(char c)
{
	return (1);
}
