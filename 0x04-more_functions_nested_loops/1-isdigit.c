#include "main.h"

/**
 * _isdigit - checks if a character is a digit
 * @c: given character to be checked
 * Return: 1 if digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

